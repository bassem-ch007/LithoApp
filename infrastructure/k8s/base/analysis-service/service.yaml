apiVersion: v1
kind: Service
metadata:
  name: analysis-service
  namespace: lithoapp
spec:
  type: ClusterIP
  selector:
    app: analysis-service
  ports:
    - name: http
      port: 8082
      targetPort: http

