apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: lithoapp
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 9090
      targetPort: http

