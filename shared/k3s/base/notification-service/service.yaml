apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: lithoapp
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8087
      targetPort: http
