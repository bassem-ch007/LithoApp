# Production patches

This directory is reserved for small production-only Kustomize patches.

The current production overlay keeps the base manifests unchanged and centralizes
application image tags in `overlays/prod/kustomization.yaml`.

