apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: lithoapp
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres

